type Species {
  id: ID!
  species_name: String!
  category: Category!
}

type Query {
  species: [Species]
}

input InputSpecies {
  species_name: String!
  category_id: ID!
}

type Mutation {
  addSpecies(input: InputSpecies): Species
}
